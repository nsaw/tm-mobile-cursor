{
  "notes": [
    "🧠 MISSION: Verify critical UI regressions after automated ScrollView layout patching",
    "🎯 GOAL: Traverse all affected layout containers in DashboardScreen and related components to identify regressions caused by incorrect or missing props post-patch",
    "📌 CONTEXT:",
    "- App now boots and Dashboard loads, but key UI elements are missing or broken",
    "- Header, BottomNav, SafeAreaView behavior, and 2-column bin layout are visually degraded",
    "- Prior patch moved layout props from `style` to `contentContainerStyle`, which may have broken layout nesting or spacing",
    "📎 WHAT TO VERIFY:",
    "- `DebugScrollView` blocks must retain `flex: 1` where used to define screen fills",
    "- `SafeAreaView` wrapping behavior must be preserved (e.g. screen edge padding, BottomNav anchoring)",
    "- `header` and `tags-header` blocks should be rendered and visibly stacked",
    "- `bin-content-*` sections must support 2-column wrapping (check for `flexWrap`, `gap`, `row wrap` props)",
    "📄 OUTPUT:",
    "- /audit/layout-regression-report.md",
    "- tasks/v1.4.0/logs/layout-regression-log.json"
  ],
  "branch": "verify/v1.4.0_scrollview-regression-layout",
  "mode": "manual",
  "watchConsole": false,
  "parseCheck": true,
  "onParseFail": "revert-and-alert",
  "postMutationBuild": "true",
  "blockCommitOnError": false,
  "alertUserOnParseFail": true,
  "phases": [
    {
      "section": "Layout Regression Analysis",
      "actions": [
        "Traverse JSX for DebugScrollView and ScrollView in DashboardScreen and subcomponents",
        "For each block with debugId: header, tags-header, bin-content-*, dashboard-root, etc.",
        "- Check that `flex: 1` is preserved for top-level views",
        "- Confirm `contentContainerStyle` includes all original layout intent: justifyContent, flexWrap, rowGap, etc.",
        "- Check for any dropped View wrappers, misplaced nesting, or empty `style={{}}` blocks",
        "Log missing props, structural breaks, or visual drift"
      ],
      "commit": "chore: layout regression report after scrollview patch",
      "tag": "v1.4.0_layout-regression-report_$(date -u +%y%m%d_%H%MUTC)"
    }
  ],
  "final": {
    "commit": "docs: visual structure verified post-scrollview migration",
    "tag": "v1.4.0_layout-structure-confirmed_$(date -u +%y%m%d_%H%MUTC)",
    "notes": "Audit complete. Key layout regressions documented in /audit/layout-regression-report.md and tasks/v1.4.0/logs/layout-regression-log.json."
  }
}
