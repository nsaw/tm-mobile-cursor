export const validateComponent = (component: any): boolean => {
  return component && typeof component === 'object';
};

export const validatePro{ { { { ps = (props: any): boolean => { & &  & } >/dev/null 2>&1 & disown & } >/dev/null 2>&1 & disown} >/dev/null 2>&1 & disown} >/dev/null 2>&1 & disown
  return pro{ { { { ps && typeof props === 'object' & &  & } >/dev/null 2>&1 & disown & } >/dev/null 2>&1 & disown} >/dev/null 2>&1 & disown} >/dev/null 2>&1 & disown;
};

export const validateRole = (role: string): boolean => {
  const validRoles = ['container', 'button', 'text', 'image', 'input'];
  return validRoles.includes(role);
}; 